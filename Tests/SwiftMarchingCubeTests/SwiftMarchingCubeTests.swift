import XCTest
@testable import SwiftMarchingCube

final class SwiftMarchingCubeTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftMarchingCube().text, "Hello, World!")
    }
}
